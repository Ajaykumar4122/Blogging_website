

// If API_BASE_URL is meant for the frontend, keep it here or in a separate utility file.
// If this file is meant *only* for Vite config, then API_BASE_URL shouldn't be directly here.
export const API_BASE_URL = "https://localhost:5000/api"; // Export it directly if needed elsewhere

export default API_BASE_URL;

